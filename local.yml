---

- hosts: localhost
  connection: local
  become: true

  tasks:
  - name: Install packages
    community.general.pacman:
      name:
        - htop
        - alacritty
      state: present
  
  - name: Install htop
    community.general.pacman:
      name: htop
      state: present

